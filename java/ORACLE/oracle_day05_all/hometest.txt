创建一个视图v_mgr_salary，列出每个主管的名字，
以及他的下属中的最高薪水和最低薪水。
CREATE VIEW V_MGR_SALARY
AS
SELECT ENAME ,MAX(SAL) MAX_SAL, MIN(SAL) MIN_SAL
FROM EMP

SELECT ENAME,SAL,DEPTNO
FROM EMP

GROUP BY DEPTNO
EVAING JOB='MANAGER'


